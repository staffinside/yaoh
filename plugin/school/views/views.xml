<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List View (replaces tree view in Odoo 18) -->
    <record id="view_student_list" model="ir.ui.view">
        <field name="name">school.student.list</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="age"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_student_form" model="ir.ui.view">
        <field name="name">school.student.form</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="age"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Window Action -->
    <record id="action_student" model="ir.actions.act_window">
        <field name="name">Students</field>
        <field name="res_model">school.student</field>
        <field name="view_mode">list,form</field>  <!-- Changed from tree to list -->
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first student</p>
        </field>
    </record>

    <!-- Menu Structure -->
    <menuitem id="menu_school_root" name="School" sequence="10"/>
    <menuitem id="menu_school_student" name="Students" parent="menu_school_root" sequence="10"/>
    <menuitem id="menu_school_student_action" name="Student List" parent="menu_school_student" action="action_student" sequence="10"/>
</odoo>